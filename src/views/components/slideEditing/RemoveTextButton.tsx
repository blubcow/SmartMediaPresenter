import React from 'react';
import RemoveButton from './RemoveButton';

const RemoveTextButton: React.FC<{}> = (props) => {
	return <RemoveButton selected={false} />;
};

export default RemoveTextButton;
